import fileName from "./helper.js";
export const lib = "es6 lib";

export const login = () => {
  console.log(fileName);
};

export default login;
// export default lib;--> error u can have only one export default fromone file
